<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Events Created:</strong>
  <ul>
  <% @user.events.each do |event| %>
    <li><%= link_to event.name, user_event_path(@user, event) %>
    </li>
  <% end %>
  </ul>
</p>

<br>
<strong><%= link_to 'Create New Event', new_user_event_path(@user) %></strong>
<br>
<br>
<br>
<%= link_to 'Back', users_path %>
